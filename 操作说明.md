# 皮影戏AI漫剧工具 - 操作说明

## 项目简介

皮影戏AI漫剧工具是一款基于 AI 技术的创意工具，能够将用户输入的故事文字自动转换为具有中国传统皮影戏风格的动画视频。

### 核心功能

- **智能分镜**：使用大语言模型将故事自动分解为多个分镜
- **角色一致性**：AI 自动生成并保持角色外观一致性
- **皮影风格**：生成具有中国传统皮影戏/剪纸艺术风格的画面
- **视频生成**：将静态图片转换为动态视频片段
- **自动合并**：自动将多个视频片段合并为完整视频

### 技术特点

- 支持最多 10 个分镜的连续剧情生成
- 并行处理加速生成速度
- 自动重试机制保证生成稳定性
- 支持 5-8 秒视频片段生成

---

## 第一部分：快速使用指南（比赛评委）

### 在线体验

直接在浏览器中打开以下链接：

```
https://sndnnnjyzgxwfeuhirj5wf.streamlit.app/
```

### 使用步骤

#### 步骤 1：访问工具

在浏览器地址栏输入上述链接，按回车键访问。

#### 步骤 2：输入故事

在页面中央的文本框中，输入你想要生成的皮影戏故事。

**示例输入**：
```
从前有七个葫芦娃，他们各有神通。大娃力大无穷，二娃千里眼顺风耳，
三娃刀枪不入，四娃喷火，五娃喷水，六娃隐身，七娃有个宝葫芦能收妖怪。
一天，妖怪抓走了爷爷，葫芦娃们决定一起去救爷爷...
```

#### 步骤 3：开始生成

点击页面上的 **「🎬 开始生成」** 按钮。

#### 步骤 4：等待完成

系统会依次执行以下步骤：

| 阶段 | 说明 | 预计耗时 |
|------|------|----------|
| 分镜生成 | 将故事分解为多个场景 | 10-30 秒 |
| 图片生成 | 为每个场景生成皮影风格图片 | 1-3 分钟 |
| 视频生成 | 将图片转换为动态视频 | 5-10 分钟 |
| 视频合并 | 将所有片段合成为完整视频 | 30 秒 |

**注意事项**：
- 请耐心等待，不要关闭浏览器页面
- 建议使用 Chrome 或 Edge 浏览器
- 确保网络连接稳定

#### 步骤 5：查看结果

生成完成后，页面会显示：
- 📸 **生成的图片**：每个分镜的预览图
- 🎬 **生成的视频**：每个分镜的视频片段

---

## 第二部分：开发者指南

### 技术栈

| 类别 | 技术 |
|------|------|
| **编程语言** | Python 3.9+ |
| **Web 框架** | Streamlit |
| **AI 模型** | 七牛云 LLM (moonshotai/kimi-k2.5) |
| **生图模型** | 可灵 AI (kling-v1) |
| **视频模型** | 可灵视频 (kling-video-o1) |
| **部署平台** | Streamlit Community Cloud |

### 项目结构

```
shadow-play-agent/
├── config.py              # 配置模块（环境变量）
├── config_template.py     # 配置模板
├── config_local.py        # 本地配置（不提交）
├── prompts.py             # AI 提示词模板
├── llm_client.py          # LLM 分镜生成模块
├── image_client.py        # 图片生成模块
├── video_client.py        # 视频生成模块
├── video_utils.py         # 视频处理工具
├── agent.py               # 主程序（工作流编排）
├── app.py                 # Web 界面
├── storyboard.py          # 分镜工具
├── requirements.txt       # Python 依赖
├── README.md              # 项目说明
├── ARCHITECTURE.md        # 架构设计文档
├── 操作说明.md            # 本文档
└── STREAMLIT_DEPLOY.md    # Streamlit 部署指南
```

### 本地部署

#### 前提条件

- Python 3.9+
- pip
- Git

#### 安装步骤

**1. 克隆仓库**
```bash
git clone https://github.com/linjwdevc/AI123.git
cd AI123
```

**2. 创建虚拟环境（推荐）**
```bash
python -m venv venv

# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate
```

**3. 安装依赖**
```bash
pip install -r requirements.txt
```

**4. 配置 API Key**

复制配置模板：
```bash
copy config_template.py config_local.py
```

编辑 `config_local.py`，填入你的 API Key：
```python
os.environ["QINIU_API_KEY"] = "你的七牛API密钥"
os.environ["KLING_IMAGE_KEY"] = "你的可灵生图密钥"
os.environ["KLING_VIDEO_KEY"] = "你的可灵视频密钥"
```

**5. 运行 Web 界面**
```bash
streamlit run app.py
```

浏览器将自动打开 http://localhost:8501

#### 命令行运行

```bash
# 测试分镜生成
python llm_client.py

# 测试图片生成
python image_client.py

# 测试视频生成
python video_client.py

# 运行完整流程
python agent.py
```

### API 配置说明

本项目使用七牛云统一 API 网关，配置文件位于 `config.py`：

```python
# 七牛云 - LLM API
QINIU_API_KEY = os.environ.get("QINIU_API_KEY", "")

# 可灵AI - 生图 API
KLING_IMAGE_KEY = os.environ.get("KLING_IMAGE_KEY", "")

# 可灵AI - 视频 API
KLING_VIDEO_KEY = os.environ.get("KLING_VIDEO_KEY", "")
```

环境变量可通过以下方式设置：
- Linux/Mac: `export QINIU_API_KEY="your-key"`
- Windows CMD: `set QINIU_API_KEY=your-key`
- Streamlit Cloud: 在 Settings → Secrets 中配置

### 测试文件

项目包含以下测试脚本：

| 文件 | 用途 |
|------|------|
| `test_character.py` | 测试角色一致性功能 |
| `test_video_flat.py` | 测试视频平面效果 |
| `test_video_single.py` | 测试单个视频生成 |

---

## 注意事项

### 使用限制

- 单次故事建议控制在 500 字以内
- 视频生成需要等待较长时间，请耐心等待
- 部分 API 有速率限制，频繁调用可能被暂时封禁

### 在线版本技术限制

Streamlit Cloud 免费版存在以下技术限制：

**技术原因**：Streamlit Cloud 的服务器采用无状态架构，部署的应用运行在临时文件系统中。当应用重启、超时或长时间无访问时，服务器上的所有本地文件（包括生成的图片和视频）都会被自动清除，无法保留和访问。

**具体表现**：
- 视频生成完成后，无法直接在网页上播放显示
- 图片也无法在网页上预览
- 用户只能看到"生成完成"的提示，但无法查看实际结果

**解决方案**：
1. 本地运行程序：下载代码到本地运行 `streamlit run app.py`，生成的文件会保存在 `output/` 文件夹中，可正常查看视频和图片
2. 云存储方案：将视频上传到 OSS 服务（如七牛云），获取公开访问 URL 后在网页上显示（需要额外配置）

**建议**：如需查看完整的视频结果，推荐使用本地部署方式（见上文"本地部署"部分）。

### 常见问题

**Q: 视频生成失败怎么办？**

A: 系统会自动重试 3 次。如果仍然失败，可以：
1. 检查 API Key 是否有效
2. 降低故事复杂度
3. 稍后重试

**Q: 图片风格不符合预期？**

A: 可以在 `prompts.py` 中调整提示词模板，优化生成效果。

**Q: 如何部署到自己的服务器？**

A: 请参考 `STREAMLIT_DEPLOY.md` 文档。

---

## 项目信息

- **项目名称**：shadow-play-agent
- **GitHub 仓库**：https://github.com/linjwdevc/AI123
- **在线体验**：https://sndnnnjyzgxwfeuhirj5wf.streamlit.app/
- **部署平台**：Streamlit Community Cloud
- **许可证**：MIT License

---

## 技术支持

如遇问题，请通过 GitHub Issues 联系：
https://github.com/linjwdevc/AI123/issues
